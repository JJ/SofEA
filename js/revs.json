{
    "language": "javascript",
   "views": {
       "rev1": {
           "map": " function(doc) {\n      var rev = doc._rev.split(\"-\");\n\tif ( rev[0] == '1' ) \n      \t\temit( doc._id, doc);\t\n    }"
       },
       "rev2": {
           "map": " function(doc) {\n      var rev = doc._rev.split(\"-\");\n\tif ( rev[0] == '2' ) \n      \t\temit( doc._id, doc);\t\n    }"
       }
   }
}